/// @description 여기에 설명 삽입
// 이 에디터에 코드를 작성할 수 있습니다
image_speed = 0;
direction = random(360);
speed = random_range(20,40);
image_index = irandom(sprite_get_number(sprite_index)-1);

angle_spd = random_range(-10,10);
alpha_spd = -random_range(0.01,0.05);